# Force LF on everything that runs on Linux/macOS — CRLF breaks bash + node shebangs.
* text=auto eol=lf
*.sh   text eol=lf
*.mjs  text eol=lf
*.json text eol=lf
# Windows-only script keeps CRLF.
*.ps1  text eol=crlf
